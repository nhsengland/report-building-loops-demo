
## Table: `r CurrentMetric`

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r , echo = FALSE, message = FALSE, warning = FALSE}

SummaryTable<- iris %>% filter(iris$Species == `CurrentMetric`)

gt(head(SummaryTable))
```

## Plot: `r CurrentMetric`

You can also embed plots, for example:

```{r , echo=FALSE, message = FALSE, warning = FALSE}
plot_ly(SummaryTable, x = ~SummaryTable$Sepal.Length, y = ~SummaryTable$Sepal.Width, type = 'scatter')
```


```{r , echo = FALSE, results='asis'}

if(`CurrentMetric` == 'setosa'){

cat("## Extra Plot", "\n \n")
}

```

```{r , echo = FALSE, echo=FALSE, warning=FALSE, message=FALSE, out.width= '100%', fig.height = 6}

if(`CurrentMetric` == 'setosa'){

plot_ly(SummaryTable, x = ~SummaryTable$Sepal.Length, y = ~SummaryTable$Sepal.Width, type = 'scatter', marker = list(color = 'purple', size = 10))
}

```
